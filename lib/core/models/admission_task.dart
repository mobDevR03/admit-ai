class AdmissionTask {
  final String title;
  final String description;
  final bool isCompleted;

  const AdmissionTask({
    required this.title,
    required this.description,
    required this.isCompleted,
  });
}